# This project has no required application secrets for local development.
# Create a Cloudflare D1 database before remote deployment, then replace the
# placeholder database_id in wrangler.jsonc with the account-specific ID.
